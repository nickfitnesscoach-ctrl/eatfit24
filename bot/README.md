# Telegram Bot - EatFit24 Personal Plan

Telegram бот для сбора анкет пользователей и генерации персонализированных планов питания/тренировок с помощью AI.

> **Архитектура**: Bot → Django API only (без прямого доступа к БД)

## 🚀 Быстрый старт

### Локальный запуск

1. **Установите зависимости:**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

2. **Настройте окружение:**
```bash
cp .env.example .env
# Отредактируйте .env и заполните необходимые переменные
```

3. **Запустите бота:**
```bash
python main.py
```

### Запуск через Docker Compose (рекомендуется)

```bash
# Из корня проекта
docker-compose up -d bot
```

## 📁 Структура проекта

```
bot/
├── app/                       # Основной код приложения
│   ├── config.py             # Конфигурация из .env
│   ├── handlers/             # Обработчики сообщений
│   │   └── survey/          # Personal Plan опрос
│   ├── keyboards/            # Клавиатуры для Telegram
│   ├── services/             # Бизнес-логика
│   │   ├── ai/              # Интеграция с OpenRouter AI
│   │   └── backend_api.py   # HTTP клиент для Django API
│   ├── states/              # FSM состояния
│   ├── texts/               # Текстовые сообщения
│   ├── utils/               # Вспомогательные утилиты
│   ├── validators/          # Валидаторы данных
│   ├── constants/           # Константы приложения
│   ├── prompts/             # Промпты для AI
│   └── assets/              # Статические ресурсы (изображения body types)
├── docs/                     # Документация
├── tests/                    # Тесты
├── main.py                   # Точка входа
├── requirements.txt          # Python зависимости
├── Dockerfile               # Docker образ
├── entrypoint.sh            # Docker entrypoint
└── .env.example             # Пример переменных окружения
```

## 🔧 Зависимости

- **Python:** 3.12+
- **aiogram:** 3.x (Telegram Bot Framework)
- **httpx:** HTTP клиент для Django API
- **tenacity:** Retry логика
- **OpenRouter API:** AI генерация планов
- **Redis:** (опционально) для FSM storage в production

## ⚙️ Переменные окружения

### Обязательные

| Переменная | Описание |
|------------|----------|
| `TELEGRAM_BOT_TOKEN` | Токен бота от @BotFather |
| `TELEGRAM_BOT_API_SECRET` | Shared secret для X-Bot-Secret (сгенерировать: `openssl rand -hex 32`) |
| `OPENROUTER_API_KEY` | API ключ OpenRouter |
| `DJANGO_API_URL` | URL Django backend (например, `http://backend:8000/api/v1`) |

### Опциональные

| Переменная | Описание | Default |
|------------|----------|---------|
| `BOT_ADMIN_ID` | ID администратора | — |
| `ADMIN_IDS` | Список ID администраторов (через запятую) | — |
| `WEB_APP_URL` | URL Telegram Mini App | — |
| `TRAINER_PANEL_BASE_URL` | URL панели тренера | — |
| `ENVIRONMENT` | `development` / `production` | `development` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `MAX_PLANS_PER_DAY` | Лимит планов в день | `3` |

Полный список см. в `.env.example`.

## 🔒 Безопасность

- Все запросы к Django API защищены `X-Bot-Secret` header
- Backend проверяет секрет в `_require_bot_secret()` 
- Без валидного секрета → 403 Forbidden

## 🧪 Тестирование

```bash
# Запустить все тесты
pytest

# С покрытием кода
pytest --cov=app tests/
```

## 📝 Логирование

Логи записываются в `logs/bot.log`.

Уровень логирования настраивается через `LOG_LEVEL` в `.env`.

## 📚 Документация

- [BOT_AUDIT.md](docs/BOT_AUDIT.md) — Архитектура и data flow
- [BOT_API_CALLS.md](docs/BOT_API_CALLS.md) — Список API вызовов
- [CHANGELOG_BOT_CLEANUP.md](docs/CHANGELOG_BOT_CLEANUP.md) — История изменений

## 🔗 Внешние ссылки

- aiogram: https://docs.aiogram.dev/
- OpenRouter API: https://openrouter.ai/docs
