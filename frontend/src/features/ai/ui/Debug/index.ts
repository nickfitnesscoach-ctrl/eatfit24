/**
 * Debug UI Components
 *
 * These components only render in debug mode (?debug=1 or localhost).
 * Used for testing and QA of the photo upload flow in browser.
 */

export { DebugPhotoControls } from './DebugPhotoControls';
export { DebugStatusPanel } from './DebugStatusPanel';
