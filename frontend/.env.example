# =============================================================================
# EatFit24 Frontend - Environment Variables Template
# =============================================================================
# This is a template file showing all available environment variables.
# Copy this file to .env.development or .env.production and fill in values.
# THIS FILE IS COMMITTED TO GIT - DO NOT PUT SECRETS HERE
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Base URL for backend API
# Development: /api/v1 (Vite proxy to localhost:8000)
# Production: /api/v1 (nginx reverse proxy to backend)
VITE_API_URL=/api/v1

# Optional: Custom trainer panel auth URL (if different from default)
# Default: /api/v1/trainer-panel/auth/
# VITE_TRAINER_PANEL_AUTH_URL=/api/v1/trainer-panel/auth/

# -----------------------------------------------------------------------------
# Telegram Configuration
# -----------------------------------------------------------------------------
# Telegram bot name (for generating Telegram links)
# Production bot: EatFit24_bot
# Note: In most cases, you don't need to set this as the app will automatically
# detect the bot name from Telegram WebApp SDK or use EatFit24_bot as default
VITE_TELEGRAM_BOT_NAME=EatFit24_bot

# Trainer invite link (shown on invite client page)
# Example: https://t.me/EatFit24_bot
VITE_TRAINER_INVITE_LINK=https://t.me/EatFit24_bot

# -----------------------------------------------------------------------------
# Development Flags (Only for .env.development)
# -----------------------------------------------------------------------------
# Mock Telegram WebApp API
# Set to '1' to enable mock Telegram API for browser testing
# Only works in development mode (import.meta.env.DEV = true)
# VITE_MOCK_TELEGRAM=1

# Skip Telegram authentication
# Set to 'true' to bypass Telegram auth check during development
# Only works in development mode (import.meta.env.DEV = true)
# VITE_SKIP_TG_AUTH=true

# -----------------------------------------------------------------------------
# Built-in Vite Variables (DO NOT DEFINE THESE)
# -----------------------------------------------------------------------------
# These are automatically provided by Vite:
# - import.meta.env.MODE: 'development' | 'production'
# - import.meta.env.DEV: boolean (true in dev)
# - import.meta.env.PROD: boolean (true in prod)
# - import.meta.env.BASE_URL: string (from vite.config.js base)

# -----------------------------------------------------------------------------
# Important Notes
# -----------------------------------------------------------------------------
# 1. All custom env variables MUST start with VITE_ prefix
# 2. Variables without VITE_ prefix will NOT be accessible in client code
# 3. Do NOT use process.env.* (not supported by Vite)
# 4. Use import.meta.env.VITE_* to access variables in code
# 5. For centralized access, import from src/config/env.ts
#
# Example:
#   import { API_BASE_URL, IS_DEV } from '@/config/env';
