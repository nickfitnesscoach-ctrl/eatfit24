╔══════════════════════════════════════════════════════════════════════════════╗
║                    DISK CLEANUP - READY TO EXECUTE                           ║
║                          EatFit24 Server                                     ║
╚══════════════════════════════════════════════════════════════════════════════╝

CURRENT STATUS:
- Disk: 76% full (37GB / 50GB)
- Can free: ~23GB (will drop to ~40% usage)
- Scripts uploaded to: /tmp/disk-cleanup-safe.sh

═══════════════════════════════════════════════════════════════════════════════

🔥 STEP 1: CONFIGURE LOG ROTATION (CRITICAL - DO THIS FIRST!)
═══════════════════════════════════════════════════════════════════════════════

SSH to server and run:

ssh deploy@85.198.81.133

Then copy-paste this ENTIRE block:

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  sudo tee /etc/docker/daemon.json > /dev/null <<'EOF'                       │
│  {                                                                           │
│    "dns": ["1.1.1.1", "8.8.8.8"],                                           │
│    "log-driver": "json-file",                                               │
│    "log-opts": {                                                            │
│      "max-size": "10m",                                                     │
│      "max-file": "3"                                                        │
│    }                                                                         │
│  }                                                                           │
│  EOF                                                                         │
│                                                                              │
│  sudo systemctl restart docker                                              │
│  sleep 5                                                                     │
│  cd /opt/eatfit24                                                           │
│  docker compose up -d                                                       │
│  docker compose ps                                                          │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

✅ EXPECTED RESULT:
   - All services should restart
   - Status: Up (healthy)
   - Log rotation now active (each container max 30MB logs)

═══════════════════════════════════════════════════════════════════════════════

🚀 STEP 2: RUN AUTOMATED CLEANUP
═══════════════════════════════════════════════════════════════════════════════

While still on the server, run:

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  bash /tmp/disk-cleanup-safe.sh                                             │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

The script will ask for confirmation at each step:
   1. Configure log rotation (already done, say 'n')
   2. Clean build cache (~12.82GB) - say 'y'
   3. Clean unused images (~5.34GB) - say 'y'
   4. Clean unused volumes - say 'y'

✅ EXPECTED RESULT:
   - Will free ~18GB
   - Disk usage drops to ~40%

═══════════════════════════════════════════════════════════════════════════════

🔍 STEP 3: VERIFY SERVICES
═══════════════════════════════════════════════════════════════════════════════

After cleanup, verify everything works:

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  docker compose ps                                                          │
│  docker logs eatfit24-backend-1 --tail=50                                   │
│  docker logs eatfit24-frontend-1 --tail=50                                  │
│  curl -H "Host: eatfit24.ru" http://localhost:8000/health/                  │
│  df -h                                                                       │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

✅ EXPECTED RESULT:
   - All containers: Up (healthy)
   - Health endpoint: {"status": "ok"}
   - Disk usage: ~40% (down from 76%)

═══════════════════════════════════════════════════════════════════════════════

💡 STEP 4 (OPTIONAL): REMOVE OLD PROJECTS
═══════════════════════════════════════════════════════════════════════════════

If you're sure you don't need youtube-media-bot, kbju_bot, foodmind:

┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  bash /tmp/remove-old-projects.sh                                           │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘

✅ WILL FREE: Additional ~5GB
⚠️  WARNING: Permanently deletes old project data

═══════════════════════════════════════════════════════════════════════════════

📊 EXPECTED FINAL STATE
═══════════════════════════════════════════════════════════════════════════════

Before:
   Disk: 37GB / 50GB (76% full)
   Docker Build Cache: 18.44GB
   Docker Images: 8.52GB
   Docker Volumes: 5.05GB

After Step 2:
   Disk: ~20GB / 50GB (~40% full)
   Docker Build Cache: ~6GB
   Docker Images: ~3GB
   Docker Volumes: ~1GB
   FREED: ~18GB ✅

After Step 4 (optional):
   Disk: ~15GB / 50GB (~30% full)
   FREED: ~23GB ✅

═══════════════════════════════════════════════════════════════════════════════

🛡️ SAFETY NOTES
═══════════════════════════════════════════════════════════════════════════════

✅ All scripts are SAFE:
   - Ask for confirmation
   - Don't delete production data
   - Don't touch PostgreSQL
   - Don't delete active volumes

❌ DON'T DO:
   - docker system prune -a (too aggressive)
   - rm -rf /var/lib/docker (breaks Docker)
   - Manually delete volumes without verification

═══════════════════════════════════════════════════════════════════════════════

📞 NEED HELP?
═══════════════════════════════════════════════════════════════════════════════

If something goes wrong:

1. Check Docker is running:
   sudo systemctl status docker

2. Check logs:
   docker logs eatfit24-backend-1 --tail=100

3. Restart services:
   cd /opt/eatfit24
   docker compose restart

4. Full restart:
   docker compose down
   docker compose up -d

═══════════════════════════════════════════════════════════════════════════════

READY TO START? 🚀

1. SSH to server: ssh deploy@85.198.81.133
2. Execute STEP 1 commands (log rotation)
3. Run STEP 2 script (cleanup)
4. Report back results!

═══════════════════════════════════════════════════════════════════════════════
